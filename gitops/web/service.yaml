apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: todo-pwa-production
  labels:
    app.kubernetes.io/name: web
    app.kubernetes.io/part-of: todo-pwa
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app.kubernetes.io/name: web
